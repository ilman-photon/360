/* eslint-disable react-hooks/rules-of-hooks */
import { useTranslation } from "react-i18next";

// export function getServerSideProps({ translation }) {
//   return {
//     props: { t: useTranslation(translation || undefined) }, // will be passed to the page component as props
//   };
// }
